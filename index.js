import { AppRegistry } from 'react-native';
import RootContainer from './src/RootContainer';

AppRegistry.registerComponent('clientfavomorn', () => RootContainer);

// in development
console.disableYellowBox = true;
